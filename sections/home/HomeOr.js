import React from 'react';
import PropTypes from 'prop-types';
import classes from './HomeOr.module.css';

const HomeOr = props => {
    return (
        <div className={classes.root}>or</div>
    );
};

HomeOr.propTypes = {

};

export default HomeOr;